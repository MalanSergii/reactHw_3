import styled from '@emotion/styled';

const FilterLabel = styled.label`
  display: block;
  margin-bottom: 30px;
`;

const FilterText = styled.span`
  margin-left: 10px;
`;

export { FilterText, FilterLabel };
